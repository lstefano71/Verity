# VSS Snapshot Mode



## Table of Contents
1. [Introduction](#introduction)
2. [Project Structure](#project-structure)
3. [Core Components](#core-components)
4. [Architecture Overview](#architecture-overview)
5. [Detailed Component Analysis](#detailed-component-analysis)
6. [Integration Patterns](#integration-patterns)
7. [Practical Examples](#practical-examples)
8. [Troubleshooting Guide](#troubleshooting-guide)
9. [Conclusion](#conclusion)

## Introduction
VSS Snapshot Mode is a feature within the Verity application that enables file integrity verification and manifest creation using Volume Shadow Copy Service (VSS) snapshots. This capability allows the application to access files that may be locked or in use by other processes by creating a point-in-time snapshot of the volume. The implementation leverages the AlphaVSS library to interface with Windows VSS, providing reliable access to file system states without interfering with active processes.

This documentation provides a comprehensive overview of the VSS Snapshot Mode implementation, including its architecture, core components, API interfaces, integration patterns, and practical usage examples. The system is designed to handle elevation requirements, snapshot lifecycle management, and path resolution to enable seamless access to shadow-copied files.

## Project Structure
The VSS Snapshot Mode functionality is implemented across several key components within the Verity project structure. The core VSS functionality resides in the Utilities folder, with integration points in the main application and comprehensive test coverage in the test project.

``mermaid
graph TD
A[Verity] --> B[Utilities]
A --> C[Services]
A --> D[Models.cs]
A --> E[Program.cs]
B --> F[VssSnapshotManager.cs]
B --> G[VssPathResolver.cs]
B --> H[ElevationHelper.cs]
I[Verity.Tests] --> J[IntegrationTests]
J --> K[VssSnapshotIntegrationTests.cs]
I --> L[VssPathResolverTests.cs]

```

**Diagram sources**
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs)
- [VssPathResolver.cs](file://Verity/Utilities/VssPathResolver.cs)
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs)
- [Program.cs](file://Verity/Program.cs)
- [Models.cs](file://Verity/Models.cs)

**Section sources**
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs)
- [VssPathResolver.cs](file://Verity/Utilities/VssPathResolver.cs)
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs)

## Core Components
The VSS Snapshot Mode implementation consists of three primary components that work together to provide snapshot functionality: VssSnapshotManager, VssPathResolver, and ElevationHelper. These components handle snapshot creation, path translation, and privilege management respectively.

The VssSnapshotManager class is responsible for creating and managing VSS snapshots, implementing IDisposable to ensure proper cleanup. The VssPathResolver translates original file paths to their corresponding locations within the snapshot. The ElevationHelper manages the privilege requirements for VSS operations, automatically requesting elevation when necessary.

``mermaid
classDiagram
class VssSnapshotManager {
+string VolumeToSnapshot
+string? SnapshotDeviceObject
+bool IsSnapshotActive
+TimeSpan SnapshotTimeout
+Task<string> CreateSnapshotAsync(CancellationToken)
+VssPathResolver CreatePathResolver()
+void Dispose()
}
class VssPathResolver {
+string SnapshotPath
+string OriginalVolumeRoot
+string ResolvePath(string originalPath)
+DirectoryInfo ResolveDirectoryInfo(DirectoryInfo original)
+FileInfo ResolveFileInfo(FileInfo original)
+bool IsPathOnSnapshotVolume(string path)
}
class ElevationHelper {
+static bool IsElevated()
+static bool RequiresElevation(CliOptions options)
+static Task<bool> RestartElevatedAsync(string[] originalArgs)
}
class VssSnapshotException {
+VssSnapshotException(string message)
+VssSnapshotException(string message, Exception innerException)
}
VssSnapshotManager --> VssPathResolver : "creates"
VssSnapshotManager --> VssSnapshotException : "throws"
Program --> ElevationHelper : "uses"
Program --> VssSnapshotManager : "uses"
```


**Diagram sources**
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs)
- [VssPathResolver.cs](file://Verity/Utilities/VssPathResolver.cs)
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs)

**Section sources**
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs#L9-L262)
- [VssPathResolver.cs](file://Verity/Utilities/VssPathResolver.cs#L5-L104)
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs#L9-L100)

## Architecture Overview
The VSS Snapshot Mode architecture follows a layered approach with clear separation of concerns. The system begins with command-line interface detection in the Program class, which triggers elevation handling when VSS mode is requested. Once elevated privileges are obtained, the VssSnapshotManager creates a volume snapshot using the AlphaVSS library, and the VssPathResolver provides transparent path translation for file access.

``mermaid
sequenceDiagram
participant User as "User"
participant Program as "Program"
participant ElevationHelper as "ElevationHelper"
participant VssManager as "VssSnapshotManager"
participant VssPathResolver as "VssPathResolver"
participant AlphaVSS as "AlphaVSS Library"
User->>Program : Execute with --vss flag
Program->>ElevationHelper : IsElevated()?
alt Not Elevated
ElevationHelper-->>Program : false
Program->>ElevationHelper : RestartElevatedAsync()
ElevationHelper->>OS : UAC Prompt (runas)
alt User Grants Elevation
OS-->>Program : Elevated Process
Program->>VssManager : Create VssSnapshotManager
VssManager->>AlphaVSS : CreateSnapshotAsync()
AlphaVSS-->>VssManager : Snapshot Created
VssManager-->>Program : Success
else User Denies Elevation
OS-->>ElevationHelper : Canceled
ElevationHelper-->>Program : false
Program->>User : Error Message
end
else Already Elevated
ElevationHelper-->>Program : true
Program->>VssManager : Create VssSnapshotManager
VssManager->>AlphaVSS : CreateSnapshotAsync()
AlphaVSS-->>VssManager : Snapshot Created
VssManager-->>Program : Success
end
Program->>VssManager : CreatePathResolver()
VssManager->>VssPathResolver : New VssPathResolver()
VssPathResolver-->>Program : Path Resolver
loop File Processing
Program->>VssPathResolver : ResolvePath(originalPath)
VssPathResolver-->>Program : snapshotPath
Program->>File System : Access via snapshotPath
end

```

**Diagram sources**
- [Program.cs](file://Verity/Program.cs#L0-L199)
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs#L9-L100)
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs#L9-L262)
- [VssPathResolver.cs](file://Verity/Utilities/VssPathResolver.cs#L5-L104)

## Detailed Component Analysis

### VssSnapshotManager Analysis
The VssSnapshotManager class is the core component responsible for managing the lifecycle of VSS snapshots. It provides an asynchronous API for snapshot creation with timeout handling and proper resource cleanup through the IDisposable pattern.

``mermaid
flowchart TD
Start([CreateSnapshotAsync]) --> CheckDisposed["Check if Disposed"]
CheckDisposed --> Disposed?{"Disposed?"}
Disposed? --> |Yes| ThrowDisposed[Throw ObjectDisposedException]
Disposed? --> |No| CheckExisting["Check Existing Snapshot"]
CheckExisting --> Existing?{"Snapshot Created?"}
Existing? --> |Yes| ThrowInvalid[Throw InvalidOperationException]
Existing? --> |No| CreateCancellationToken["Create Timeout CTS"]
CreateCancellationToken --> CreateCombinedCts["Create Combined CTS"]
CreateCombinedCts --> RunInternal["Task.Run CreateSnapshotInternal"]
RunInternal --> Success?{"Operation Successful?"}
Success? --> |Yes| SetCreated["Set _snapshotCreated = true"]
SetCreated --> ReturnPath["Return SnapshotDeviceObject"]
Success? --> |No| Timeout?{"Timeout CTS Cancelled?"}
Timeout? --> |Yes| ThrowTimeout[Throw VssSnapshotException<br/>"timed out"]
Timeout? --> |No| ThrowGeneric[Throw VssSnapshotException<br/>"Failed to create"]
ReturnPath --> End([Return])
ThrowDisposed --> End
ThrowInvalid --> End
ThrowTimeout --> End
ThrowGeneric --> End
```


**Diagram sources**
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs#L61-L86)

**Section sources**
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs#L61-L86)

### VssPathResolver Analysis
The VssPathResolver component handles the translation of original file paths to their corresponding locations within the VSS snapshot. This enables transparent access to files in the snapshot without requiring changes to file processing logic.

``mermaid
flowchart TD
Start([ResolvePath]) --> ValidateInput["Validate originalPath"]
ValidateInput --> NullOrEmpty?{"NullOrEmpty?"}
NullOrEmpty? --> |Yes| ThrowNull[Throw ArgumentNullException]
NullOrEmpty? --> |No| GetFullPath["Path.GetFullPath()"]
GetFullPath --> OnSameVolume?{"Path on Same Volume?"}
OnSameVolume? --> |No| ReturnFullPath["Return fullOriginalPath"]
OnSameVolume? --> |Yes| ExtractRelative["Substring OriginalVolumeRoot"]
ExtractRelative --> ConstructPath["Path.Combine(SnapshotPath, relativePath)"]
ConstructPath --> ReturnSnapshot["Return snapshotFilePath"]
ReturnFullPath --> End([Return])
ReturnSnapshot --> End
ThrowNull --> End

```

**Diagram sources**
- [VssPathResolver.cs](file://Verity/Utilities/VssPathResolver.cs#L34-L61)

**Section sources**
- [VssPathResolver.cs](file://Verity/Utilities/VssPathResolver.cs#L34-L61)

### ElevationHelper Analysis
The ElevationHelper class manages the privilege requirements for VSS operations, automatically handling the elevation process when required. This ensures that VSS operations can proceed with the necessary administrative privileges.

``mermaid
flowchart TD
Start([RestartElevatedAsync]) --> GetProcessPath["Get Environment.ProcessPath"]
GetProcessPath --> CreateProcessInfo["Create ProcessStartInfo"]
CreateProcessInfo --> SetFileName["FileName = processPath"]
SetFileName --> SetArguments["Arguments = originalArgs"]
SetArguments --> SetVerb["Verb = runas"]
SetVerb --> SetUseShellExecute["UseShellExecute = true"]
SetUseShellExecute --> StartProcess["Process.Start()"]
StartProcess --> ProcessStarted?{"Process Started?"}
ProcessStarted? --> |Yes| Delay["Task.Delay(1000)"]
Delay --> ReturnTrue["Return true"]
ProcessStarted? --> |No| HandleException["Catch Win32Exception"]
HandleException --> Cancelled?{"NativeErrorCode == 1223?"}
Cancelled? --> |Yes| ReturnFalse["Return false"]
Cancelled? --> |No| ReturnFalse
ReturnTrue --> End([Return])
ReturnFalse --> End
```


**Diagram sources**
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs#L50-L100)

**Section sources**
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs#L50-L100)

## Integration Patterns
The VSS Snapshot Mode is integrated into the Verity application through command-line options and programmatic configuration. The system follows a consistent pattern across different commands (verify, create, add) to enable VSS functionality.

### Command-Line Integration
VSS mode is activated by passing the --vss or -vss flag to any of the main commands. The Program class detects this flag and initiates the elevation process if necessary.

``mermaid
sequenceDiagram
participant CLI as "Command Line"
participant Program as "Program"
participant Options as "CliOptions"
CLI->>Program : verify manifest.txt --vss
Program->>Program : Check args for --vss
Program->>Program : vssRequested = true
Program->>ElevationHelper : IsElevated()
ElevationHelper-->>Program : false
Program->>ElevationHelper : RestartElevatedAsync()
ElevationHelper->>OS : UAC Prompt
OS-->>Program : Elevated Process
Program->>Options : new CliOptions(..., UseVss=true)
Options-->>Program : Options with VSS enabled

```

**Diagram sources**
- [Program.cs](file://Verity/Program.cs#L0-L199)
- [Models.cs](file://Verity/Models.cs#L0-L9)

**Section sources**
- [Program.cs](file://Verity/Program.cs#L0-L199)
- [Models.cs](file://Verity/Models.cs#L0-L9)

### Programmatic Integration
The VSS functionality can also be used programmatically by directly instantiating the VssSnapshotManager and VssPathResolver classes. This pattern is used within the verification and manifest creation workflows.

```
csharp
// Example of programmatic integration
var volumeRoot = Path.GetPathRoot(rootPath);
using var vssManager = new VssSnapshotManager(volumeRoot);
await vssManager.CreateSnapshotAsync();
var vssResolver = vssManager.CreatePathResolver();

// Use resolver to access files in snapshot
var snapshotPath = vssResolver.ResolvePath(originalPath);
var fileInfo = vssResolver.ResolveFileInfo(originalFileInfo);

```

**Section sources**
- [Program.cs](file://Verity/Program.cs#L150-L199)

## Practical Examples

### Basic Usage
To use VSS Snapshot Mode, simply add the --vss flag to any Verity command:

```
bash
# Verify files using VSS snapshot
verity verify manifest.txt --vss

# Create manifest using VSS snapshot
verity create new_manifest.txt --vss

# Add files to manifest using VSS snapshot
verity add existing_manifest.txt --vss

```

### Programmatic Implementation
Here's an example of how to use the VSS components programmatically:

```
csharp
try
{
    // Determine volume to snapshot
    var rootPath = options.RootDirectory?.FullName ?? options.ChecksumFile.DirectoryName!;
    var volumeRoot = Path.GetPathRoot(rootPath);
    
    if (string.IsNullOrEmpty(volumeRoot))
    {
        Console.WriteLine("Error: Unable to determine volume root for VSS snapshot.");
        return (int)VssErrorCode.VolumeNotSupported;
    }
    
    // Create and use VSS snapshot
    using var vssManager = new VssSnapshotManager(volumeRoot);
    await vssManager.CreateSnapshotAsync();
    
    // Create path resolver for translating paths
    var vssResolver = vssManager.CreatePathResolver();
    
    // Process files using snapshot paths
    foreach (var file in filesToProcess)
    {
        var snapshotPath = vssResolver.ResolvePath(file.OriginalPath);
        // Process file at snapshotPath
        var hash = ComputeHash(snapshotPath);
    }
}
catch (VssSnapshotException ex)
{
    Console.WriteLine($"VSS Error: {ex.Message}");
    return (int)VssErrorCode.SnapshotCreationFailed;
}
catch (UnauthorizedAccessException)
{
    Console.WriteLine("Access denied. Ensure the application is running with administrator privileges.");
    return (int)VssErrorCode.ElevationRequired;
}

```

### Error Handling Patterns
The VSS implementation includes comprehensive error handling for common scenarios:

```
csharp
// Handle elevation requirements
if (options.UseVss && !ElevationHelper.IsElevated())
{
    var restarted = await ElevationHelper.RestartElevatedAsync(args);
    if (!restarted)
    {
        Environment.Exit((int)VssErrorCode.ElevationRequired);
    }
}

// Handle snapshot creation with timeout
try
{
    using var vssManager = new VssSnapshotManager(volumeRoot, TimeSpan.FromMinutes(2));
    await vssManager.CreateSnapshotAsync(cancellationToken);
}
catch (VssSnapshotException ex) when (ex.Message.Contains("timed out"))
{
    Console.WriteLine("Snapshot creation timed out. The volume may be under heavy load.");
}
catch (VssSnapshotException ex)
{
    Console.WriteLine($"Failed to create snapshot: {ex.Message}");
}

```

**Section sources**
- [Program.cs](file://Verity/Program.cs#L0-L199)
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs)
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs)

## Troubleshooting Guide
This section addresses common issues encountered when using VSS Snapshot Mode and provides solutions for each scenario.

### Elevation Issues
**Problem**: Application fails to create VSS snapshot with access denied errors.

**Solution**: Ensure the application is running with administrator privileges. When using the --vss flag, the application will automatically request elevation. If the UAC prompt is denied, the operation will fail.

```
csharp
// Check elevation status
if (!ElevationHelper.IsElevated())
{
    Console.WriteLine("Application requires administrator privileges for VSS operations.");
}

```

**Section sources**
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs#L15-L30)
- [Program.cs](file://Verity/Program.cs#L0-L50)

### Snapshot Creation Failures
**Problem**: VssSnapshotManager.CreateSnapshotAsync() throws VssSnapshotException.

**Common Causes and Solutions**:
- **Timeout**: Increase the snapshot timeout period or ensure the system is not under heavy load.
- **Volume not supported**: Some volume types (e.g., network drives) do not support VSS snapshots.
- **Insufficient disk space**: Ensure adequate free space for shadow copies in the system settings.

```
csharp
// Handle timeout specifically
var vssManager = new VssSnapshotManager(volumeRoot, TimeSpan.FromMinutes(10));

```

**Section sources**
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs#L74-L109)

### Path Resolution Issues
**Problem**: Resolved snapshot paths do not point to valid files.

**Solution**: Verify that the original path is on the same volume as the snapshot. The VssPathResolver only translates paths for the snapshotted volume.

```
csharp
// Check if path is on snapshot volume
var resolver = new VssPathResolver(snapshotPath, volumeRoot);
if (resolver.IsPathOnSnapshotVolume(originalPath))
{
    var snapshotPath = resolver.ResolvePath(originalPath);
    // Path is valid for snapshot access
}
else
{
    // Path is on different volume, use original path
    var originalPath = Path.GetFullPath(originalPath);
}

```

**Section sources**
- [VssPathResolver.cs](file://Verity/Utilities/VssPathResolver.cs#L84-L104)

### Resource Cleanup
**Problem**: VSS snapshots not being properly cleaned up.

**Solution**: Always use VssSnapshotManager within a using statement to ensure proper disposal:

```
csharp
using var vssManager = new VssSnapshotManager(volumeRoot);
await vssManager.CreateSnapshotAsync();
// Snapshot automatically cleaned up when disposed
```

The VssSnapshotManager implements IDisposable and will clean up the snapshot when disposed, even if an exception occurs.

**Section sources**
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs#L186-L262)

## Conclusion
VSS Snapshot Mode provides a robust solution for accessing locked or in-use files through Volume Shadow Copy Service snapshots. The implementation is well-structured with clear separation of concerns between snapshot management, path resolution, and privilege handling.

Key features include:
- Automatic elevation handling for administrative privileges
- Asynchronous snapshot creation with timeout support
- Transparent path translation for seamless file access
- Proper resource cleanup through IDisposable pattern
- Comprehensive error handling for common failure scenarios

The system integrates smoothly with the Verity application through command-line flags and can also be used programmatically for custom workflows. By leveraging the AlphaVSS library, the implementation provides reliable access to VSS functionality while abstracting away the complexity of the underlying Windows APIs.

For optimal use, ensure the application is run with appropriate privileges, handle exceptions appropriately, and always use proper disposal patterns to prevent resource leaks.

**Referenced Files in This Document**   
- [VssSnapshotManager.cs](file://Verity/Utilities/VssSnapshotManager.cs)
- [VssPathResolver.cs](file://Verity/Utilities/VssPathResolver.cs)
- [ElevationHelper.cs](file://Verity/Utilities/ElevationHelper.cs)
- [Program.cs](file://Verity/Program.cs)
- [Models.cs](file://Verity/Models.cs)
- [VssSnapshotIntegrationTests.cs](file://Verity.Tests/VssSnapshotIntegrationTests.cs)
- [VssPathResolverTests.cs](file://Verity.Tests/VssPathResolverTests.cs)